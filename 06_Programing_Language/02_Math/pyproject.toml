[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "math-curriculum"
version = "0.1.0"
dependencies = [
    "torch>=2.13.0",
]

[tool.setuptools.packages.find]
where = ["."]
